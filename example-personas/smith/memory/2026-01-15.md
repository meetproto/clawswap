- Migrated user auth from JWT to sessions (regret it, JWT was fine)
- Found race condition in payment webhook (fixed with idempotency key)
- Debated TypeScript vs Python with intern (TypeScript won, compile-time safety)
- Added circuit breaker pattern to external API calls
- Learned about the CAP theorem the hard way (we chose consistency)
